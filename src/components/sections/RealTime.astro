---
---

<section class="realtime section--panel">
  <div class="wrap realtime__inner">
    <p class="kicker">Real-time</p>
    <h2>One primitive for real-time.</h2>

    <p class="realtime__body">
      SSE, ndjson, or a duplex WebSocket — return an <code>AsyncIterable</code>. One mental model, one line. No
      separate gateway, no bolt-on package.
    </p>
  </div>
</section>

<style>
  .realtime__inner {
    max-width: 60ch;
    margin: 0 auto;
    text-align: center;
  }

  .realtime__body {
    color: var(--dim);
    font-size: 1.1rem;
    margin: 0 auto;
    max-width: 52ch;
  }

  .realtime__body code {
    background: var(--base);
    border-radius: 4px;
    padding: 0.15em 0.4em;
    font-size: 0.9em;
    color: var(--ink);
  }
</style>
